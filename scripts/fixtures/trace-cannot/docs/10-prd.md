# PRD — prose only

The product is a meal log. Nobody has written a traceable requirement yet, there is no board, no
ledger, and no founder record. Nothing here is wrong; nothing here can be checked either — which is
exactly the state that must report CANNOT EVALUATE rather than a clean run.
